services:
  frontend:
    build: 
      context: ./frontend
      dockerfile: ./docker/Dockerfile
    ports:
      - 5173:5173
    volumes:
    - ./frontend:/app
  backend:
    build: 
      context: ./backend
      dockerfile: ./docker/Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./backend:/app
